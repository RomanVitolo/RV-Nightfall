# Handoff: The Quiet Village — UHFPS multiplayer conversion

I'm continuing work on my Unity game with you. Read this whole brief before doing anything, then read `Assets/QuietVillage/Docs/UHFPS-PATCHES.md`, which is the authoritative record of every change made to UHFPS. Verify anything below against the code before relying on it: this is a summary, and the code wins where they disagree.

## The project

- **Repo:** `C:\UnityProjects\VyR\RV-NightFall`, Unity project in `The Quiet Village/`. GitHub: `RomanVitolo/RV-Nightfall` (public). Branch `main`, see `git log` for the latest state.
- **Engine:** Unity 6000.4.1f1. Netcode for GameObjects 2.13.2, Unity Multiplayer Services (sessions/Relay), Cinemachine 3.1.7, TextMeshPro, UI Toolkit for the lobby.
- **Game:** a first-person co-op horror game, 2–4 players, built on **UHFPS** (ThunderWire Studio's single-player horror template) converted to multiplayer. Levels: listed in `Assets/QuietVillage/Data/LevelCatalog.asset` (currently greybox `ForestVillage` and `CoastalTown`, plus UHFPS's demo `GameplayScene`); lobby: `LobbyScene`.
- **Git LFS:** `Assets/Sci_Fi_Super_Pack` (~25 GB of textures and models, including files over 100 MB) is stored in LFS. Cloning needs Git LFS installed. A local-only branch `backup/before-lfs-migrate` still exists and holds ~24 GB of old data; it can be deleted.

## Project layout

Everything first-party lives in `Assets/QuietVillage/`; vendor content (`ThunderWire Studio`, `Sci_Fi_Super_Pack`, `Plugins`, `PluginMaster`, `TextMesh Pro`) stays at the Assets root. Editor tools read paths from `QuietVillage.ProjectPaths` (`Scripts/Editor/ProjectPaths.cs`), the one place to change if folders move.

```
Assets/QuietVillage/
  Scenes/          LobbyScene, ForestVillage, CoastalTown, GameplayScene (UHFPS demo, adapted); NavMesh/
  Scripts/
    Multiplayer/Runtime   asmdef QuietVillage.Multiplayer.Runtime   namespace QuietVillage.Multiplayer.* (incl. Characters/)
    Multiplayer/Bridge    Assembly-CSharp                           namespace QuietVillage.Multiplayer.Bridge.*
    Gameplay/Survival     Assembly-CSharp                           namespace QuietVillage.Gameplay.Survival
    Editor/               Assembly-CSharp-Editor: Multiplayer/ (QuietVillage.Multiplayer.Bridge.EditorTools),
                          Gameplay/ (QuietVillage.Gameplay.EditorTools), ProjectPaths.cs
  Prefabs/         Player/, Multiplayer/ (MultiplayerRoot, LobbyUI), Creatures/, Items/Drops/
  Data/            LevelCatalog.asset, CharacterCatalog.asset; Resources/AvatarHeldItemModels.asset
  Art/             Animations/Worker, Materials/Greybox, Skybox
  UI/Multiplayer/  lobby and overlay UXML/USS/panel settings
  Docs/            UHFPS-PATCHES.md
```

`Assets/DefaultNetworkPrefabs.asset` stays at the root: Netcode looks for it there. Only the multiplayer runtime has an asmdef; gameplay, bridge and tools stay in Assembly-CSharp because UHFPS does (an asmdef cannot reference Assembly-CSharp). Splitting further means giving UHFPS asmdefs first. All tools are under **Tools > Quiet Village**.

## Architecture (important rules)

- **UHFPS is third-party code**, in `Assets/ThunderWire Studio/UHFPS` (Assembly-CSharp). It's patched in **72 files**, each marked `MULTIPLAYER PATCH` or using `LocalPlayerContext`. Check with:
  `grep -rl "MULTIPLAYER PATCH\|LocalPlayerContext" "Assets/ThunderWire Studio" --include=*.cs | wc -l` → must be **72**.
  Prefer adding code in the bridge over patching UHFPS. Every new UHFPS patch must be documented in `UHFPS-PATCHES.md`, with the count updated.
- **Bridge** (`Assets/QuietVillage/Scripts/Multiplayer/Bridge/`, Assembly-CSharp) glues UHFPS to Netcode. It may reference UHFPS and the module.
- **Module** (`Assets/QuietVillage/Scripts/Multiplayer/Runtime/`, asmdef `QuietVillage.Multiplayer.Runtime`) holds sessions, lobby, flow and spawning. **It cannot reference UHFPS or the bridge** (asmdef → Assembly-CSharp is impossible). When the module needs something from the bridge, define an interface plus a static holder in the module and have the bridge register (examples: `ISaveCatalog`/`SaveCatalog`, `ISpawnGate`/`SpawnGate`).
- **Authority:** the owner runs its own player (UHFPS first-person stack). Player health is server-authoritative (`PlayerHealthSync`). The host simulates NPCs (`NetworkedNpc`). World objects replicate through one in-scene `WorldSync` NetworkBehaviour; each object is a `WorldSyncEntity` with a stable id (a GlobalObjectId written by **Tools > Quiet Village > Multiplayer > Set Up World Sync**, which works on the open level; re-run it after adding objects to the level).
- **Levels:** one scene per settlement. `LevelCatalog` (module, ScriptableObject) is the single list of playable levels; a scene not in it is never treated as a level. The host picks the level in the lobby's Create Room dialog (or it follows the chosen save), it's published as a session property (`SessionService.RoomLevel`), and `SessionFlow` loads it through Netcode. Restart reloads the active level; `NetworkPlayerSpawner` spawns into any catalog level. Levels are identified by **scene name**, so names must be unique.
  - **Tools > Quiet Village > Levels > Create New Level...** makes a scene with GameplayScene's `GAMEMANAGER` systems copied in, a 100 m greybox floor and a moonlight, then sets it up.
  - **Tools > Quiet Village > Levels > Set Up Open Scene As Level** (idempotent): session guard, removes stray NetworkManager/SessionService, SceneGameReferences, spawn points, World Sync, Build Settings, catalog entry.
  - Rename levels or add descriptions via **Select Level Catalog**.
- **Players:** `GameManager`, `Inventory`, `PlayerPresenceManager`, `SaveGameManager` and other managers live on the player prefab (`Assets/QuietVillage/Prefabs/Player/NetworkedWorkerPlayer.prefab`, which nests UHFPS's `HEROPLAYER`). `LocalPlayerContext` is the one global for "this client's player". `HeroPlayerNetworkSetup` splits owner from remote copies: remote copies have UHFPS components, audio, cameras and HUD switched off, and a third-person avatar (Sci-Fi Worker, humanoid rig) shown instead.
- **Runtime-added components:** most bridge features are added in code by `HeroPlayerNetworkSetup`, not placed on the prefab, so the prefab rarely needs editing.
- **Rooms lock when the game starts:** there's no joining mid-game and no rejoining.

## Features done (all compile; none has been playtested with two or more players)

1. **Health through the host.** Healing, damage zones, fall damage and item attributes all request changes from the host (`IPlayerHealthAuthority`, patch in `PlayerHealth`). Also fixed a `PlayerHealth.Awake` crash that had broken medkits.
2. **Menus safe in multiplayer** (`SessionMenus`). Load and Restart are hidden. Quit/Main Menu become Leave Game (End Game for the host), with a confirm on the second press. The level exit (`LevelInteract`) and UHFPS scene loads are blocked with a hint (`ISessionExit`, patch in `GameManager`).
3. **Death** (`SessionDeathScreen`, `SpectatorCamera`). A killing blow first puts a player down (feature 23); death itself is permanent for that player object, though a survival level brings the dead back at dawn (feature 22). Dead players watch a living teammate, with Next Player to switch. When everyone is dead, the host gets Restart Level (`SessionFlow.RestartLevel`, a networked reload) and clients see "Waiting for the host". Player display names are synced (`HeroPlayerNetworkSetup.DisplayName`).
4. **Dropped items shared** (`DroppedItems`, patch in `Inventory.ContextHandler`). The host allocates World Sync keys, everyone creates a copy, the dropper's physics streams the fall, and the first taker wins.
5. **Carried items survive death or disconnect** (`CarriedItems`). A dying player drops everything droppable; the host drops a disconnected player's items where they last stood. Requires item drop data: **Tools > Quiet Village > Multiplayer > Set Up Droppable Items** (already run) gave 22 non-player items a drop prefab (`Assets/QuietVillage/Prefabs/Items/Drops`) and Object References entry. Player items (flashlight, weapons) are not droppable, so they are lost on death.
6. **Teammate sounds** (`AvatarSounds`): footsteps (the player's own FootstepsSystem settings and the surface underfoot), gunshots, melee swings, draw/holster sounds, flashlight clicks, and reload and other item-animation sounds (patch in `AnimationSoundEvent`, forwarded by name through `PlayerActionSync`).
7. **Held items visible:** `PlayerActionSync` publishes the equipped item index and whether its light is on. `AvatarHeldLight` shines a teammate's flashlight, lantern or candle; `AvatarHeldItem` puts a mesh-only copy of the item's pickup prefab in the avatar's right hand. Models and per-item grips live in `Assets/QuietVillage/Data/Resources/AvatarHeldItemModels.asset`, built by **Tools > Quiet Village > Multiplayer > Set Up Held Item Models** (UHFPS's demo database leaves `Item.ItemObject` empty, so it can't be the source). **The grip offsets are untuned guesses**: edit them per item in that asset.
8. **Name tags** (`AvatarNameTag`): world-space TMP text above teammates, hidden by walls, fading out by 25 m, and hidden when that player is dead.
9. **Crouch synced** (`PlayerLocomotionSync.IsCrouching`). Remote copies lower their camera holder, so zombie sight and the spectator camera are correct and footsteps use the real crouch state. The avatar body still stands, because the pack has no crouch clips.
10. **"Host left" notice:** `SessionService.SessionEndedReason` plus a dismissible banner in the lobby (`LobbyScreen`, `LobbyScreen.uxml`, `Multiplayer.uss`) when a room ends without the player choosing it.
11. **Co-op save/resume** (`Scripts/Multiplayer/Bridge/Saves/*`, `WorldSync.Saves.cs`, no UHFPS changes).
    - **Saving:** host-only Save Game in the pause menu. The host gathers each client's inventory, objectives, health and position (`PlayerSaveState`), keyed by **Unity account id** (`SessionService.LocalPlayerId`). It saves the world keyed by World Sync ids (`WorldSyncEntity.SaveTarget`), plus dropped items, into UHFPS's save folder format.
    - **Resuming:** in the lobby's Create Room dialog, pick New game or a save. Each client is sent the world and its own slice before its player spawns (`ISpawnGate`). Newcomers start empty-handed. Restart Level after a wipe returns to the latest save.
    - **Why not UHFPS's own save path:** it can't work, because `SaveGameManager`'s world-saveable list is scene references that a prefab can't hold.
    - **One save per level:** with UHFPS's `SingleSave` setting (on), co-op saves go to `Save<SceneName>`, so saving in one settlement no longer overwrites another. With it off, folders are numbered from the highest existing one.
    - **Health** resumes server-side: `PlayerHealthSync` starts each player at their saved health (minimum 1) via `WorldSync.TryGetSavedHealth`.
    - **Other game systems** join the save through `IWorldSaveParticipant` (register with `WorldSync.RegisterSaveParticipant` on the host; read back with `WorldSync.TryGetResumedState`). Stored under `participants` in the data file.
    - **Not saved:** NPCs, so zombies reset alive when a save is resumed.
12. **Survival loop prototype** (`Assets/QuietVillage/Scripts/Gameplay/Survival/`, Assembly-CSharp, namespace `QuietVillage.Gameplay.Survival`, no UHFPS changes).
    - `SurvivalDirector` (one in-scene NetworkObject per level, server-authoritative): day/night clock replicated as phase + server end time; barricade health as one `NetworkList<int>` indexed by `Barricade.Order`; spawns `NightCreature`s at night (base + per living player, growing each night) and despawns them at dawn. A run is several nights (feature 22). Host can press **F8** to skip the current phase (testing).
    - `Barricade`: UHFPS timed interaction (hold Use). Client asks the host; host adds 50 health (max 100) and tells the builder's client to spend 1 **Scrap** (UHFPS's existing item, standing in for wood). Standing = solid collider + carving `NavMeshObstacle`.
    - `NightCreature`: host-only NavMeshAgent. Chases the nearest player with a complete NavMesh path; if barricades cut every path, attacks the nearest standing barricade. Invulnerable; audio from feature 18. Body from the creature catalog (feature 14); the capsule remains as the fallback.
    - `DayNightLighting` (sun angle, dusk, fog/ambient) and `SurvivalHud` (clock, scrap, barricades standing) run on every client from replicated state.
    - **Greybox settlements:** **Tools > Quiet Village > Build Greybox > Forest Village / Coastal Town** (`SettlementGreyboxBuilder`, layouts in code, rebuildable). Builds buildings, shelter openings with barricades, trees, scrap, creature/player spawns, the director, a baked NavMesh (`NavMesh-<Level>.asset`), creates `Assets/QuietVillage/Prefabs/Creatures/NightCreature.prefab` (registered in DefaultNetworkPrefabs), then runs level setup.
    - **Saved:** phase, time left and barricade health (`SurvivalDirector` is an `IWorldSaveParticipant`). Creatures are not; a night resumes with a fresh set at the spawn points.
13. **Characters and roles** (`Scripts/Multiplayer/Runtime/Characters/*`, `UI/CharacterScreen.cs`, bridge `LocalRolePerks`, no UHFPS changes).
    - **Design choices:** each character *is* a role; duplicates are allowed; chosen on a Character screen reachable from the lobby header and from the waiting room (until the host starts); a resumed save restores each player's character.
    - **Catalog:** `CharacterCatalog` (module ScriptableObject, `Data/CharacterCatalog.asset`) lists characters: id, name, role, description, Humanoid Avatar, body scale, look variants (vendor prefabs), perks and starting items (UHFPS item GUIDs as strings). Ids are stored in saves and PlayerPrefs, so don't change them. Seeded roster: **Builder** (Sci-Fi Worker, +50% barricade repair, 2 Scrap), **Medic** (Sci_Fi_Character_05, +50% healing, ×2 revive speed, First Aid Kit), **Scout** (Head Hunter, +15% run speed, Lockpick), **Scavenger** (Seller, +9 inventory slots). All numbers are untuned guesses.
    - **Choice flow:** `CharacterSelections` (on MultiplayerRoot) keeps the local choice in PlayerPrefs, shows it to the room as a session player property (display only, roster role labels), and sends it to the host as a Netcode **named message** on connect and on change. The host's record is authoritative for spawning. `NetworkPlayerSpawner` asks `ChoiceFor(clientId)` (saved character → chosen → default) and assigns it to `PlayerCharacter` **before** spawning; `PlayerCharacter` sends it inside the spawn (`OnSynchronize`), so every client knows it in its first `OnNetworkSpawn`.
    - **Bodies:** the prefab still nests the Worker. On remote copies `HeroPlayerNetworkSetup.ApplyCharacterBody` runs first and `RemoteAvatarBinder.ReplaceBody` swaps in the character's prefab via `CharacterBodies.Create` (shared WorkerAnimator, the character's Avatar, no root motion, AlwaysAnimate), copying `AvatarAim` settings. The lobby preview (`CharacterPreviewStage`, in LobbyScene at y = -500, renders to a RenderTexture only while the screen is open) uses the same recipe.
    - **Perks:** healing (`PlayerHealthSync.RequestHealRpc`) and barricade repair (`SurvivalDirector.BuildRpc`) are applied on the host from `PlayerCharacter.Perks`. Run speed, extra slots (`Inventory.ExpandInventory`) and starting items are applied by the owner (`LocalRolePerks`); slots and items only on a fresh start (not when restored from a save).
    - **Saves:** `PlayerSaveState` stores `"character"` in each player slice; `WorldSync` implements `ISavedCharacterSource` (registered like `SpawnGate`).
    - **Character screen in a room:** a host resuming a save publishes each saved player's character as the `savedCharacters` room property (`ISaveCatalog.ResumedCharacters` → `SessionService.SavedCharacterOf`, display only). A player in that save sees a notice, a locked roster and a Back button instead of Done; their PlayerPrefs pick is left alone. Room rows and the header use the saved character when there is one. Roster cards show a "Current" tag and "Played by …" for teammates; Done is disabled until the character, look or name changes.
    - **Character screen controls:** cards show a short perk summary (`CharacterCatalog.Summarise`). Up/Down (arrows or D-pad, via `NavigationMoveEvent`) changes character, Left/Right cycles looks, Escape/controller cancel closes, Enter confirms from a card or the name field. The chosen card gets focus on open. Random picks any other character and look (hidden when a save locks the roster). The details column shrinks to 300px before the preview loses space. Controller submit (or Enter) on a card confirms (`NavigationSubmitEvent`, caught trickling down so the card does not treat it as a click); a mouse click still only selects.
    - **Starting item icons:** `StartingItem.Icon` (Sprite), copied from the UHFPS item's `Icon` by Set Up Characters when empty (matched by GUID, so existing entries get one too). Shown beside each item under "Starts with".
    - **Look thumbnails:** `CharacterPreviewStage.Thumbnail` renders a head-and-shoulders picture of each look into its own RenderTexture (`m_thumbnailResolution`, 128 px), one per frame, by enabling a second camera at a spot 40 m to the side (URP does not support `Camera.Render`). Cached for the session. The Look buttons show the picture above the look's name.
    - **Preview mood** (`CharacterPreviewStage`, Mood header): a night cemetery behind the body. Opaque night-blue background, a ground plane (`m_groundMaterial`, the grassy cemetery ground), 14 Old Cemetery props (`m_backdrop`: trees, kneeling angel, gravestones, mound, candle trio, grass; colliders removed, pack lights and scripts disabled), a moonlight that casts soft shadows, a dimmer rim light and a warm candle point light. Fog and flat ambient light are scene-wide, so the stage applies them only while the screen is open and restores the lobby's own settings on close. Set Up Characters seeds the backdrop and ground only when both are empty; after that, positions are hand-editable in the Inspector. `m_moodEnabled` off returns to the old transparent look. **All placements, colours and the fog density are first guesses, not seen rendered.**
    - **Body scales:** Medic 0.79 and Scavenger 0.84 (from the setup tool's fit report); all four bodies now fit the 1.8 m collider.
    - **Setup (characters):** **Tools > Quiet Village > Characters > Set Up Characters** (idempotent): creates/seeds the catalog only when empty, fills missing Avatars and item GUIDs by title, reports body height vs the collider, adds `PlayerCharacter` to the player prefab, `CharacterSelections` to MultiplayerRoot, and the preview stage to LobbyScene. **Select Character Catalog** to edit roles and perks.
14. **Creature bodies** (`Gameplay/Survival/CreatureCatalog.cs`, `CreatureBody.cs`, changes in `NightCreature` and `SurvivalDirector`, editor `Gameplay/CreatureSetup.cs`; no UHFPS changes, no vendor asset changes).
    - **Packs:** `Assets/HorrorMaiden` (Humanoid, zombie materials) and `Assets/Super_Mega_Creatures_Pack` (Generic rigs, each creature with its own controller and clips).
    - **Catalog:** `Data/CreatureCatalog.asset` lists bodies: prefab, clips (idle, walk, run, up to 3 attacks, death, entrance), generated override controller, optional Avatar, scale, yaw offset, material swaps, agent speed, clip ground speeds, attack hit delay/duration, death duration, Enabled. Selection mode: **Mixed** (random enabled body per creature), **Cycle**, or **Only One** (by id).
    - **Animation:** every body plays `Art/Animations/Creatures/CreatureBase.controller` (1D locomotion on `MoveBlend` idle/walk/run with `MoveTimeScale`, attack blend on `AttackVariant`, `Entrance`, `Dead`) through its own `AnimatorOverrideController` in `Overrides/`. Gait clips that don't loop in their import are copied, looping, into `Loops/` (the pack imports clips with Unity defaults, which don't loop).
    - **Networking:** the director instantiates, calls `NightCreature.AssignBody(index)`, then spawns; the index travels in the spawn (`OnSynchronize`), like `PlayerCharacter`. `CreatureBody` (added at spawn on every client) builds the model, strips vendor colliders/scripts/rigidbodies, hides the capsule renderers, and derives locomotion from transform movement (nothing extra replicated). Attacks: `PlayAttackRpc` to everyone; the host lands damage after `AttackHitDelay` if the target is still in reach and unobstructed, and the agent stands still for `AttackDuration`. Dawn: `DieAtDawn` sets a server NetworkVariable (death anim) and despawns after `DeathDuration`.
    - **Testing:** host **F9** switches all creatures (current and future) to the next usable body, enabled or not, with a hint naming it. F8 still skips the phase.
    - **Setup:** **Tools > Quiet Village > Creatures > Set Up Creatures** builds the base controller, seeds the catalog only when empty (maiden zombies from hand-picked clips; creature pack prefabs by clip-name matching; humanoid creatures whose pack controller is missing borrow `Animations_For_Humanoid/Set_01`), rebuilds overrides from the clips every run, scales bodies taller than 3 m or shorter than 1 m toward human size, and assigns the catalog to `NightCreature.prefab`. Seeded: 56 usable, 15 enabled (both maidens, Creepy, Hunter, Creature Mutant, Creature Humanoid 01–05, Monster 01–05).
    - **Zombie Female animations** (`Assets/ZombieFemaleAnimations`, Humanoid; editor `CreatureSetup.ZombieFemale.cs`): **Tools > Quiet Village > Creatures > Apply Zombie Female Animations** (re-runnable; replaces clips, speeds and timings of its targets). Targets: both Horror Maidens, the package's Female Zombie (added as `zombie_female`), Creature Humanoid 01–05 and Monster 01–05; Creepy, Hunter and Mutant keep their own clips. Clips rotate per body (5 idles, 6 walks, 2 jogs + 4 sprints, 3 attack sets of in-place/combo attacks, 3 deaths, screams as entrances); sprinters hunt at 3.8 m/s, joggers 3.2. The pack's Generic creatures get a Humanoid Avatar from a copy of their model in `Creatures/Rigs` (same UE-Mannequin skeleton); where Unity's auto-mapping fails (Creature Humanoid 01–04), the bone mapping is borrowed from one that mapped and the skeleton taken from the model's own bones. The package marks every clip as looping, so deaths are copied non-looping (`Loops/*_once.anim`). Its duplicate `RamsterZCameraController.cs` (same class as HorrorMaiden's) was deleted; its demo scene points at HorrorMaiden's copy.
    - **Don't** enable looping in the pack's own import settings: it gives the default take a new internal id and breaks the pack's controllers (this happened once and was repaired).

15. **Old cemetery levels** (`Assets/Old Cemetery` pack with its URP package imported; editor `SettlementGreyboxBuilder.Cemetery.cs`; runtime `Gameplay/Environment/FollowPlayerCamera.cs`; no UHFPS changes, no vendor asset changes).
    - **Design choices:** dress the existing layouts rather than redesign them; both levels, each with its own character; renamed in the lobby (scene names unchanged, so saves and the catalog still match); rain only in the seaside level.
    - **Levels:** `ForestVillage` is now **Hollow Chapel Cemetery** (grassy ground, fenced plots along an avenue to the north crypt, old headstone clusters in dense dead trees, heavy still fog). `CoastalTown` is now **Saltmarsh Cemetery** (earth ground, open grave fields, a crypt street, a north-wall plot, rain, the sea beyond the sea wall).
    - **Build:** **Tools > Quiet Village > Build Greybox > Forest Village / Coastal Town** (menu names unchanged) rebuilds the greybox and then, when the layout has a `CemeteryStyle`, dresses it before the NavMesh bake. The shelter becomes a stone chapel (masonry texture, steep slate gable roof with stepped gables and a cross; the flat roof stays as an invisible collider), houses become crypts (same treatment, grave pots at the door), the level edge becomes an iron fence with a double gate (the old boundary walls stay as invisible colliders), plus paving, grave plots (slab + headstone, mound + old stone, or monument), fences, angels, dead trees (inside and a treeline outside on a 160 m ground), candles (each has a flickering light; kept few), ivy and grass. Weather per level is written into `DayNightLighting`, and the pack's skybox is used.
    - **Keeping play intact:** a keep-clear set (building margins, the ground before every door and barricade attack point, creature and player spawns, loose scrap, paths, the gate) is respected by every placement; plots skip cells that would crowd it. After the bake, `CheckCemeteryPaths` verifies every creature spawn reaches every barricade and player spawn and every crypt door is reachable, and warns otherwise (both levels pass). Solid pieces get box colliders sized from the mesh (the pack's models ship without); grass, ivy, candles and paving are walk-through.
    - **Pack quirks handled in code:** `AngelStatue-LODs.prefab` references two materials missing from the pack (empty slots are filled per instance); the URP unitypackage must be imported or every material is magenta; rain is the pack's `ParticleEffect-RAIN` under a `Weather` object with `FollowPlayerCamera` (world-space particles).
    - **Rebuilding** regenerates scrap and barricades, so World Sync ids for those change: existing co-op saves of these two levels may not restore scrap and barricades correctly.

16. **Day activities** (`Gameplay/Survival/LootSpot.cs`, `CryptPadlock.cs`, `Floodlight.cs`, `GeneratorControl.cs`, `SurvivalDirector.Loot.cs`, `SurvivalDirector.Power.cs`, changes in `SurvivalDirector`, `NightCreature`, `SurvivalHud`; editor `SettlementGreyboxBuilder.Day.cs`; no UHFPS changes, no vendor asset changes).
    - **Design choices:** search for loot, locked crypts, generator and floodlights; props inside and outside; loot changes each new game.
    - **Random loot:** the builder places many pickups, each with a `LootSpot` (group, keep count, guaranteed). The host picks a seed (`SurvivalDirector.m_lootSeed`, a NetworkVariable, saved as `lootSeed`); every client sorts each group by World Sync id, shuffles it with the seed and deactivates what isn't kept. Nothing else travels. The greybox's own house and loose scrap is not a `LootSpot` and is always there. Groups: each crypt (open crypts keep 1 of 4, locked ones 3), `chapel` (1 of 3), `outdoors` (5–6 of 12–14), `fuel` (2 of 4 canisters), one per key (1 of 4 hiding places), one per padlock note (1 of 3).
    - **Locked crypts:** only locked crypts get a UHFPS door (`DynamicDoor_Locked1`), fitted into the 1.6 m opening with stone jambs and a head; its leaf carves the NavMesh while shut. Forest: crypts 1 and 5 by key (Key 01/02), 3 padlock, 4 lockpick. Coast: 2 and 8 by key, 4 padlock, 6 lockpick. The lockpick door copies UHFPS's chest `LockpickInteract` settings with the door pick model and the Lockpick item as bobby pin. The padlock is `Puzzle_Padlock` on the door's outer face; `CryptPadlock` sets its code from the seed and writes it on the `Paper_B` notes. Keys and notes hide only in open crypts or outdoors.
    - **Generator and floodlights** (server-authoritative, in the director): one `PowerState` NetworkVariable (running, fuel, server time). Fuel burns only while running at night; 20 s to start, 60 s per canister, 180 s max (inspector). Refuel: hold Use on the fuel cap with a canister (spent by the owner, handed back if the host refuses); press Use on the body to switch. UHFPS's `PowerGenerator` is not used (it drains per client and churns World Sync). Four floodlights outside the chapel corners; each `Floodlight` covers a ground circle sized by the builder so every barricade attack point is inside one (radii 5–10 m). Saved as `generatorRunning`/`generatorFuel`.
    - **Creatures and light:** a creature standing in lit ground backs out to its edge and pauses 1.5 s; players and barricade attack points in light are ignored as targets; if every standing barricade is lit, creatures prowl at the light's edge nearest one.
    - **Props:** crypts get a cabinet with drawers (synced), a crate with a candle and a barrel against the walls; the chapel gets crates in its corners (the quietest corner holds the generator); 12–16 crate stacks and barrel pairs outside on free ground (physics overlap check plus the cemetery keep-clear). Crates and barrels are stripped of drag, physics and title, so they're static scenery baked into the NavMesh. Storage trunks and breakable crates are deliberately not used (trunk contents duplicate when their state is re-applied; crates roll loot per client).
    - **HUD:** adds `Canisters N · Generator ON/off m:ss`.

17. **Puzzle turn-taking** (bridge `World/IPuzzleGate.cs`, `World/SyncedPuzzleLock.cs`; patches in `PuzzleBase`, `PuzzleBaseBlend`, both already patched, so still 72 files). Close-up puzzles (padlock, keypad, electrical circuit, maze, safe keypad, examine puzzles) take a host-granted lock through `WorldSync.RequestLock`, like `SyncedExamineLock`: a second player is told "Someone else is using this." The lock is released on leaving the puzzle, when the puzzle object is switched off, after 1.5 s if a grant goes unused, and by the host on disconnect. **Set Up World Sync** adds one per `PuzzleBase`/`PuzzleBaseBlend` (id kind `puzzle`); run on ForestVillage, CoastalTown and GameplayScene (1, 1 and 7 added; existing ids unchanged). Single-press puzzles (keycard, fusebox) and the lockpick and safe dial are not locked.
    - Also fixed: the crypt padlock left its door locked when opened by aiming at the padlock itself (`CryptPadlock` now unlocks the door on `OnPadlockUnlock`); padlock notes threw in `InteractableItem.Start` (GString built without a key); `GameManager.Awake` threw in `RainingModule.OnAwake` before the scene's post-processing volume was bound, skipping the dialogue system and default blur radius (`GetStack`/`SetStack` null-guarded; `GameManager.BindPostProcessing` called from `HeroPlayerNetworkSetup`).
18. **Creature audio** (`Gameplay/Survival/CreatureSounds.cs`, `CreatureVoice.cs`, changes in `NightCreature`, `CreatureCatalog.Body.Sounds`, `SurvivalDirector`; editor `CreatureSetup.Sounds.cs`). `Data/CreatureSounds.asset` (seeded only while empty): 7 UHFPS zombie grunts, 4 parasite screams/roars, 6 whooshes and claws, 3 deaths, 10 UHFPS dirt footsteps, 4 barricade hits and UHFPS `WoodBreak`. Assigned to `NightCreature.prefab` (`m_sounds`, only when unset); a catalog body can override with its own. `CreatureVoice` (added at spawn on every client, 3D logarithmic, 2–40 m): growls every 4–10 s (local timing), footsteps from distance actually moved, a staggered scream on rising, swing sounds from `PlayAttackRpc`, death from the dying NetworkVariable. The host sends `PlaySpottedRpc` when a creature first finds a reachable player (6 s host rate limit, 14 s scream cooldown). Barricade hits and breaks play from the replicated health list on every client (no message).
19. **Roles in the in-game room panel** (`SessionOverlay`, F1): each player's role beside their name. `CharacterSelections.RoomChoiceOf`/`RoomRoleOf` (saved character when resuming, else their pick) now serve both the lobby and the overlay.
20. **Tuning (reasoned, not playtested):** generator 0 s to start, 40 s per canister, 120 s tank (was 20/60/180, which lit whole nights); Builder barricade repair ×2 (at ×1.5 a new barricade still took 2 Scrap, so the perk only helped repairs); Scout run speed +20% (was +15%). Medic and Scavenger unchanged. Held-item grips on non-Worker bodies are **not** tuned: they need eyes on each body in Play mode.

21. **Game settings in the waiting room** (module `Sessions/GameplaySettings.cs`, `UI/GameplaySettingsPanel.cs`, changes in `SessionService`, `LobbyScreen`, `ISaveCatalog`/`MultiplayerSaveCatalog`; gameplay `SurvivalDirector.Settings.cs`, changes in `SurvivalDirector`, `.Loot`, `.Power`, `NightCreature`; no UHFPS changes).
    - **Design choices:** nights to survive, day length, night length, creature count, damage and speed, loot amount, fuel per canister; Easy / Normal / Hard / Custom presets (Custom shows sliders); the host edits them in the waiting room until the game starts, everyone else sees a summary; a resumed save keeps its settings, shown locked.
    - **Values** are scales of the level as built (1 = as built), except fuel in seconds per canister and nights (1–10). Normal: all 1, fuel 40, 4 nights. Easy: day 1.25, night 0.75, creatures 0.6, damage 0.6, speed 0.85, loot 1.4, fuel 55, 3 nights. Hard: day 0.8, night 1.25, creatures 1.5, damage 1.4, speed 1.15, loot 0.7, fuel 30, 5 nights. Settings without `nights` (older saves and prefs) play Normal's 4. Ranges are clamped on read (`GameplaySettings.*Range`). All preset numbers are untuned guesses.
    - **Flow:** room property `settings` (encoded `preset=…;day=…`), plus `settingsLocked` when resuming a save. `SessionService.SetRoomSettings` (host) coalesces saves like character changes and keeps a pending copy so sliders don't jump. A new room starts from the host's last settings (PlayerPrefs `QuietVillage.GameplaySettings`); a resumed one from the save's (`survival.settings`, Normal for older saves).
    - **In the level:** the host's director reads `SessionService.RoomSettings` on spawn (Normal with the level's own fuel value outside a room). Day/night lengths and the loot share go to clients as NetworkVariables in the spawn (the clock, lighting and the loot seed need them). Host only: creature count (never below 1), damage (never below 1), agent speed, fuel per canister. Loot keep counts scale per group with the fraction settled by the seeded random; `key:` and `note:` groups never scale. Settings are saved with the director's state.

22. **Multi-night runs** (`Gameplay/Survival/SurvivalDirector.Run.cs`, `RunEndScreen.cs`, changes in `SurvivalDirector`, `.Settings`, `SurvivalHud`, `GameplaySettings`, `GameplaySettingsPanel`; module `NetworkPlayerSpawner.Respawn`, `PlayerCharacter.Returning`; bridge `SessionDeathScreen.WipeHandledByLevel`; no UHFPS changes).
    - **Design choices:** nights by preset (Easy 3 / Normal 4 / Hard 5, Custom slider); a dawn supply drop rather than re-rolled loot; the dead return at dawn if anyone survived the night; one end screen for win and wipe.
    - **Phases:** Day → Night → Dawn (`m_dawnDuration`, 15 s) → Day … and the last Night → `Won` (new `SurvivalPhase` value; the clock stops). `m_night` (from 1, still the survived night during its dawn) and `m_totalNights` are NetworkVariables; `night` is saved (older saves resume on night 1). Creature count × (1 + `m_creatureGrowthPerNight` (0.35) × (night − 1)). Hints and HUD show "NIGHT 2/4" etc.
    - **Dawn (host):** downed players are revived (`PlayerHealthSync.ServerRevive`); dead players' objects are despawned and, a frame later, respawned through `NetworkPlayerSpawner.Respawn` with `PlayerCharacter.Returning` set (travels in the spawn): 40 health (`m_returnHealth`), no starter kit, no saved health, spawn gate bypassed, placed on their spawn point. The supply round (`m_supplyRound`) moves on.
    - **Supplies:** each client claims the round for its own player once they are standing and have been spawned ≥ 1 s: `m_dawnScrap` (2) Scrap and, with a generator, `m_dawnCanisters` (1) canister. Rounds from before a client's spawn are not claimable.
    - **End of run (host):** `Won` or `PlayerHealthSync.EveryoneDead` sets `m_nightsSurvived`, `m_summary` (FixedString4096: per player repairs, revives, downed, deaths, counted from `PlayerHealthSync.ServerDowned/ServerRevived/ServerDied` and `BuildRpc`), then `m_outcome`. `RunEndScreen` (every client, code-built uGUI, opens 3 s later, frees the cursor, freezes the player): host gets **Play Again** (new game: `SaveCatalog.StartFresh` + `SessionFlow.RestartLevel`) and **End Game**, or after losing a resumed save **Retry from Save / New Game / End Game**; clients get **Leave Game**. While a director exists, `SessionDeathScreen` shows no Restart of its own on a wipe.
    - **Not done:** returning to the waiting room with the room kept (End Game closes it); stats are not saved; a returning player loses their objectives (UHFPS `ObjectiveManager` lives on the player).
23. **Downed and revive** (bridge `PlayerHealthSync`, new `DownedPlayer.cs`, `ReviveTarget.cs`, changes in `HeroPlayerNetworkSetup`, `AvatarNameTag`, `CharacterCatalog.Perks.ReviveSpeed`; gameplay `NightCreature`; no UHFPS changes).
    - **Design choices:** a killing blow downs the player for 30 s; a teammate holds Use about 4 s to revive at 30 health (Medic ×2 speed); any hit while down, the timer running out, or nobody left standing kills.
    - **State in one value:** `m_health` > 0 standing, 0 downed, −1 dead, so no client can see "health 0, not downed" and run UHFPS's irreversible death early. `Health` clamps at 0; `IsDead`, `IsDowned`, `IsStanding`; `m_bleedOutAt` (server time) for countdowns. The owner mirrors a downed player's health to UHFPS as 1. With no one else standing (or `m_downedEnabled` off), a killing blow kills at once. Healing never revives.
    - **Owner (`DownedPlayer`):** holsters and disables items (`PlayerItemsManager.IsItemsUsable`), disables `InteractController`, disables the Idle/Walk/Run/Jump/Sliding states and forces UHFPS's Crouch at 0.8 m/s (restored on revive); red overlay with the bleed-out countdown.
    - **Others:** remote bodies play their death animation while down (health reads 0), the name tag turns red with "DOWN 24", a hint announces who went down. `ReviveTarget` (child of the remote body, Interact layer, trigger box 2.2 × 0.8 × 2.2 m, on only while down) is a UHFPS timed interaction; the host checks the reviver is someone else, standing and within 3.5 m.
    - **Creatures** target standing players first and downed ones only when no standing player is reachable.
    - **Medic:** `Perks.ReviveHealth` (×2) multiplies the health of players they revive (60 instead of 30), besides `ReviveSpeed` ×2.
24. **Permadeath option** (`GameplaySettings.DeadReturn`, room key `return`): Easy/Normal on, Hard off; a toggle under Custom. When off, `SurvivalDirector.BeginDawn` does not return the dead and the dawn hint drops "The fallen return." Older settings strings play on.
25. **Fighting back without killing** (`Gameplay/Survival/SurvivalDirector.Tools.cs`, `ChapelBell.cs`, `BarricadeTrap.cs`, `FlareVisual.cs`, `FadeAndDestroy.cs`, `SynthSounds.cs`, `SurvivalProps.cs`, `CreatureHitbox.cs`, changes in `NightCreature`, `CreatureBody`, `CreatureVoice`, `SurvivalDirector.Power`; editor `Gameplay/SurvivalToolsSetup.cs`, loot tables in `SettlementGreyboxBuilder.Day`; no UHFPS code changes).
    - **Creature effects (host):** `NightCreature.Stun(s)` (replicated as `m_stunnedUntil` server time; the body's animator plays at 25% and it growls high), `Slow(factor, s)`, `Repel(from, s)` (walks 6 m away, ignores targets), `CallTo(player, s)`. Stuns cancel a swing mid-way.
    - **Weapon stagger:** `CreatureHitbox` (`IDamagable`, added at spawn to every solid collider; the capsule is on the child `Body`) turns any UHFPS weapon hit on the shooter's client into `RequestStaggerRpc`: 1 s stun, then 2.5 s immune. Damage is ignored. Axe (weight 8) and pistol (5) are now in locked-crypt loot, pistol ammo (6) in open loot. No noise mechanic exists, so shots are not loud to creatures.
    - **Flare** (inventory item, custom use event): lights a 6 m circle for 25 s at the player's feet (`m_flares` NetworkList of position + server end time). `IsLit`/`EdgeOfLight` treat it like floodlight at any phase. Red flickering light, emissive stick, synthesized crackle. 1 flare in each dawn supply; loot weight 12 in open loot.
    - **Holy Water** (inventory item): stuns 2 s and repels for 5 s every creature within 6 m; blue light pulse and hiss. Loot weight 5 open, 15 locked.
    - **Chapel bell** (`ChapelBell`, placed at runtime on every client at the ground under the average barricade position, i.e. the shelter's middle): hold Use 1 s; stuns every creature within 25 m for 4 s; 120 s rest (`m_bellReadyAt`, replicated). Primitive frame + bronze bell, trigger collider on Interact. Synthesized bell heard to 90 m.
    - **Scrap traps** (`BarricadeTrap`, one per barricade at its attack point, runtime-placed): hold Use 2 s outside, costs 2 Scrap (spent first, refunded if refused); the first creature within 1.3 m at night is stunned 2 s and slowed to 50% for 4 s more, and the trap is spent. `m_traps` NetworkList<byte>, saved as `traps`.
    - **Items setup:** **Tools > Quiet Village > Survival > Set Up Tool Items** (already run) added `Flare` (GUID `25f4c388…`) and `Holy Water` (`2d46868e…`) to the inventory database (section of Healing Potion; titles matched at runtime by `SurvivalDirector.FlareTitle/HolyWaterTitle`; localization keys `*Title`), made unpacked pickups `Prefabs/Items/Pickups/Pickup_Flare` (from the candle pickup) and `Pickup_HolyWater` (from the healing potion), then ran Set Up Droppable Items (drops made for both; Object References now 24). **The levels were not rebuilt**: flares/holy water/axe/pistol only appear as loot after Build Greybox again (which changes World Sync ids, as noted in feature 15).
26. **Role abilities** (key **G**; module `CharacterCatalog.RoleAbility` + `Character.Ability`, `DescribeAbility` shown first on the Character screen; gameplay `RoleAbilities.cs` (client, on the director), `SurvivalDirector.Abilities.cs` (host); setup seeds them).
    - **Builder – Reinforce:** aim at or stand within 3.5 m of a barricade; host sets it to 150 health (`m_reinforcedShare`; refused while someone stands in an empty opening). Once per day-night cycle (comes back when the next Day starts; tracked against `Night`). Saves keep reinforced health.
    - **Medic – Healing Area:** host heals standing players within 5 m by 20 × Healing perk (30 for the Medic); green pulse + chime. 90 s cooldown.
    - **Scout – Sense Creatures:** 8 s of red `[!]` screen markers with distance over every creature, through walls (local only). 60 s cooldown; refused without spending when no creatures exist.
    - **Scavenger – Scrounge:** +1 Scrap into their own inventory, once per cycle.
    - HUD line under the supplies: `[G] Reinforce · Ready`. Cooldowns are client-timed (not validated on host); the host validates the character's ability. State lives on the director, so returning at dawn does not reset it.
27. **Creature behaviours** (`CreatureCatalog.CreatureBehaviour` on each `Body`): **Brute** (speed ×0.75, barricade damage ×3, player damage ×1.25, stuns halved) — Monster 01–05; **Stalker** (speed ×1.25, targets the standing player furthest from any teammate, 0.5 s light retreat instead of 1.5) — Creepy, Hunter, Creature Mutant; **Screamer** (player damage ×0.7; on spotting a player calls creatures within 20 m to that target for 8 s) — both Horror Maidens, Female Zombie; **Standard** — Creature Humanoid 01–05 and every disabled body. Set in the catalog asset by hand; Set Up Creatures does not seed them (it only seeds an empty catalog).

28. **Menus and loading screens** (module `UI/LobbyScreen.cs`, new `UI/MenuSettingsPanel.cs`, `Settings/DisplaySettings.cs`, `Loading/LoadingScreen.cs`, `Loading/LoadingScreenContent.cs`, `Flow/BootLoader.cs`, `LevelCatalog.Level.LoadingImage`; UI `MultiplayerRuntimeTheme.tss`, `Multiplayer.uss`, `LobbyScreen.uxml`, `UI/Resources/Loading/*`; editor `Multiplayer/LoadingScreensSetup.cs`; no UHFPS changes).
    - **Readability:** the theme now styles every text input, dropdown (including its open list, which lives at the panel root and so must be styled in the theme, not the layout's USS), slider, toggle and scrollbar: dark input background, near-white 20px text, white caret, red focus border and selection. Body font Roboto Condensed (theme `:root`), titles and menu buttons Noto Serif Bold, both from UHFPS's font folders by path. Field labels are 190px wide; hints under fields line up with the inputs.
    - **Main menu:** LobbyScene opens on `menu-view` (Play → room browser, Character, Settings, Quit; "Playing as NAME · Role"; version). The browser has "‹ Main Menu". Being in a room always shows the waiting room; leaving a room returns to the browser. The header (name field, Character button) is hidden on the menu.
    - **Create Room:** two columns: the form (with a Show password toggle) and a live "Room preview" card (name, level and its description, max players, access, new game or save, host, the settings the room will start with). Enter creates, Escape cancels (join dialog too).
    - **Settings:** resolution, display mode (fullscreen / borderless / windowed), quality level, VSync, master volume (heard live, reverted on Close without Apply). Saved in PlayerPrefs `QuietVillage.Settings.*`, applied before the first scene loads (resolution only in builds). UHFPS's in-game Options read the current values when they have no file, so they agree; once a player saves in-game options, UHFPS's file wins inside levels.
    - **Boot splash:** `Scenes/BootScene` (first in Build Settings; black camera + `BootLoader`) shows the splash for at least 2.5 s while LobbyScene loads, then fades out over the menu. Opening LobbyScene directly still works.
    - **Level loading screen:** `LoadingScreen` creates itself (DontDestroyOnLoad, own UIDocument, panel sorting 1000, assets in `Resources/Loading`). It follows Netcode scene events: when this client starts loading a catalog level (start or restart) it shows the level name/description (and `LoadingImage` if set), "YOU ARE" role card (role, body, G ability, perks; saved character when resuming), a rotating tip from `LoadingScreenContent` (14 defaults, editable), and progress (scene load → "Placing your player"/"Waiting for the host" → Ready). Hides once the local player object exists + 0.5 s and at least 3 s total; hides at once if the session stops, gives up after 60 s. To extend: add a section to `LoadingScreen.uxml`, fill it in `LoadingScreen.FillLevel`.
    - **Setup:** **Tools > Quiet Village > UI > Set Up Boot And Loading Screens** (already run): LoadingPanelSettings (copy of the lobby's, sorting 1000), LoadingScreenContent, BootScene, Build Settings order.

29. **Player UI events repaired** (editor `Multiplayer/PlayerEventRepair.cs`, **Tools > Quiet Village > Multiplayer > Repair Player UI Events**, already run; `SessionMenus.Calls`). The HUD's move onto the player prefab had left 7 persistent listeners with no target (pause Resume, both Main Menu buttons, Save Game, the death screen's Restart, Options Apply, the inventory's blocker click), so those buttons did nothing and `SessionDeathScreen` logged "does not have the expected Restart button" and dead players never spectated. The tool gives each empty listener the prefab's single component of the type it names. `SessionMenus`/`SessionDeathScreen` also accept a listener with a missing target by method name, so a future break cannot silently disable them again.

## Known gaps and possible next work

- **Feature 28 (menus, loading):** not seen rendered; compiled and assets built only. Check in Play mode from BootScene: splash fades into the menu; menu buttons; type a name in the Character screen and a room name (text clearly visible, caret visible); open dropdowns (list dark and readable); Create Room preview updates while typing; Settings volume and quality apply; start a 2-player game and watch the loading screen on both (role card, tips, progress, hides when you can move); restart after a wipe shows it again. Watch for: fonts not loading (USS `url("/Assets/...")` paths with spaces), the loading panel blocking clicks on the menu after it hides, the menu layout at 1280×720, and UHFPS's own scene-start fade overlapping.

- **Features 24–27:** untested; nothing was seen rendered. Two-player steps: (1) Waiting room: Hard shows "Permadeath"; Custom has the "Dead return at dawn" toggle. (2) Host F8 to night; Builder presses G at a barricade → 150%, HUD shows "Used until next day"; Medic presses G beside a hurt teammate → green pulse, health up; Scout presses G → red markers through walls; Scavenger G → +1 Scrap. (3) Give yourself a Flare/Holy Water (Dawn supply gives a flare): use a flare outside at night → creatures back off its red circle; holy water beside a creature → it freezes, then walks away. (4) Ring the bell in the chapel → loud bell, creatures within 25 m freeze 4 s, title shows the rest timer on both clients. (5) Set a trap outside a barricade (2 Scrap) → spikes appear for both; a creature attacking there freezes and then moves slowly; spikes gone. (6) Rebuild a level, find an axe in a locked crypt, hit a creature → brief freeze, not again for 2.5 s. (7) F9 through bodies: a Monster breaks a barricade in ~5 s; a Hunter goes for the player standing alone; a Maiden's scream pulls the others. Watch for: bell placement (inside a prop, or in a doorway), trap spots under barricade debris, flare/effect lights' cost, the `[!]` marker font, G conflicting with anything, weapons' raycast masks actually hitting the creature collider (layer Default), and animator speed 0.25 looking like a freeze rather than a flinch.
- **Primitives:** bell, traps and flares are placeholder shapes; replace with models in `ChapelBell.Create`, `BarricadeTrap.Create`, `FlareVisual.Create`. Sounds are synthesized (`SynthSounds`); swap for clips when there are some.

- **Multi-night runs and revives (features 22–23):** untested. Two-player steps: (1) Create a room on Normal, check the waiting room shows "4 nights" and Custom has a Nights slider. (2) Start; the HUD reads DAY 1/4. Host F8 to night; let player B get hit to 0 — B crawls with the red overlay and countdown, cannot use items; A sees the red "DOWN" tag and hint, holds Use on B's body; B stands at 30. (3) Get B down again and wait 30 s — B dies and spectates. (4) F8 to dawn: B respawns empty-handed at 40 health on a spawn point, both get 2 Scrap (+1 canister), HUD shows the dawn countdown, next day reads DAY 2/4 and night 2 has more creatures. (5) Down A while B is dead/absent — A dies at once. (6) Wipe: both die → end screen after 3 s with 1 of 4 (or 0) nights, host Play Again reloads fresh, client sees Waiting. (7) Custom 1 night, survive → YOU SURVIVED with stats. Watch for: the crawl camera on uneven ground, the downed avatar's box being hard to aim at, the cursor staying free on the end screen, an EventSystem present for the buttons, and whether the respawned player's camera/HUD binds correctly (the risky part).

- **Game settings:** untested. Check the panel fits the waiting room at 1280×720, that a client sees changes live, that Custom sliders are smooth while the host drags, that a resumed save shows the lock, and that Hard/Easy visibly change creature count, speed and loot in a level.

- **Held-item grips per body:** grips live per item in `AvatarHeldItemModels.asset`, tuned on the Worker. Medic, Scout and Scavenger rigs likely hold items at wrong angles; tune in Play mode, or add a per-character grip correction.
- **Creature audio and puzzle locks:** untested. Check volumes and ranges in fog, that growls do not stack unpleasantly with many creatures, footstep cadence at sprint, and that a second player gets the hint (not a stuck camera) when trying a puzzle in use.

- **Day activities:** not playtested, and nothing was seen rendered. Check: prop facing and placement (the cabinet is assumed to face +Z, crates may clip walls), that pickups sit on surfaces rather than inside them, the door fit and which way it swings, whether the padlock hangs on the door and its puzzle camera frames it, that the lockpick minigame opens from the door, the floodlight model's aim, whether floodlight radii make nights too easy (tune fuel values), creatures jittering at the light's edge when their path crosses it, and extra candle lights inside crypts. Rebuilding changed World Sync ids, so older saves of both levels will not restore correctly.


- **Cemetery levels:** not playtested. Check at night: candle light count and cost, rain density and whether it clips through roofs, fog strength per level, crypt interiors (still plain boxes inside), and whether any grave/fence spot is annoying to move around. Buildings are still box geometry under the new roofs; real chapel/crypt models would be the next step.

- **Creatures:** every body is untested in play. Expect per-body tuning: facing (`YawOffset`), scale, feet sliding (clip speeds), hit timing, and whether the Humanoid Avatars built for Creature Humanoid 01–05 and Monster 01–05 retarget cleanly (01–04 were mapped from their bind pose, which may not be a T-pose, so watch for twisted arms). The NavMeshAgent radius/height and the capsule collider are the same for every body.
- **Repository size:** HorrorMaiden (2 GB) and Super_Mega_Creatures_Pack (13 GB) are not covered by `.gitattributes` LFS rules; add rules before committing them.

- **Playtest pass with 2–3 players.** Nothing above has been verified in play. Highest priority.
- Tune the held-item grip offsets. They were tuned (guessed) on the Worker; other bodies' hands may need their own.
- Characters: tune perk numbers and body scales; hand-pick nicer looks per role (the seeded bodies were chosen by folder name, not by eye); roles now show in the in-game room panel (feature 19).
- Save NPC state (zombie alive/dead and position).
- Level transitions mid-session (UHFPS level exits still refuse; levels are chosen in the lobby only).
- UHFPS's `MainMenu`, `LevelManager`, `Elevator` and `Raining` scenes are still in Build Settings, unused.
- Rejoining after a disconnect (needs a full world snapshot for returning players).
- Voice chat (Vivox), optional.
- Crouch animation for the avatar (needs clips).
- Custom item data (for example a gun's loaded ammo) is lost on drop, as in UHFPS.
- New UI strings are English-only constants.

## How to work in this project

- **Compile check** (Unity closed; the timeout message at the end is normal):
  ```
  "/c/Program Files/Unity/Hub/Editor/6000.4.1f1/Editor/Unity.exe" -batchmode -nographics -quit -projectPath "C:/UnityProjects/VyR/RV-NightFall/The Quiet Village" -logFile <scratchpad>/compile.log
  ```
  Then `grep -c "error CS" compile.log` → must be 0.
- **Editing files from shell:** long bash heredocs containing C# have failed to parse here. Use the file write/edit tools, or Python (`py`) with anchored string replacements. Python writes LF line endings. Don't leave whole-file line-ending churn on untouched files; revert with `git checkout -- <file>`.
- **Code style:** match the existing bridge code. XML doc comments explain *why* (authority, ordering, what breaks otherwise), `m_` fields, clear early returns. Components are added at runtime where possible.
- **Before big features, confirm design choices with me.** So far I chose:
  - host-only saving;
  - saves keyed by player account;
  - resume chosen in the lobby;
  - permadeath with spectating;
  - host restarts when everyone is dead;
  - each character is a role, duplicates allowed, chosen in the lobby (editable while waiting), kept in saves.
- **Don't commit or push unless I ask.** When you do, end commit messages with the attribution line I give you.
- **Always tell me what's untested,** and give concrete two-player test steps.
